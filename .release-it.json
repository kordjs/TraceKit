{
  "$schema": "https://unpkg.com/release-it@19/schema/release-it.json",

  "git": {
    "commitMessage": "chore(release): v${version}",
    "tag": true,
    "tagName": "v${version}",
    "requireCleanWorkingDir": true,
    "changelog": "npx auto-changelog --stdout --starting-version 2.1.0"
  },

  "npm": {
    "publish": false
  },

  "github": {
    "tokenRef": "GITHUB_TOKEN",
    "release": true,
    "releaseName": "v${version}"
  },

  "hooks": {
    "after:bump": "npx auto-changelog --starting-version 2.1.0"
  }
}
